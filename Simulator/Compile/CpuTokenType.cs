namespace Simulator.Compile {
    internal enum CpuTokenType {
        // ReSharper disable once UnusedMember.Global
        None,
        Instruction,
        Value,
        Label,
        Data
    }
}